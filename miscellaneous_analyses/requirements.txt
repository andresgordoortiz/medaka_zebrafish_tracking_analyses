# =============================================================================
# Python dependencies for ultrack_viewer.py (napari 4D embryo viewer)
# =============================================================================
#
# Single requirements file compatible with pip, uv, pipenv, and mamba/conda.
#
# Install with your tool of choice:
#
#   pip / pip-tools
#     pip install -r requirements.txt
#
#   uv
#     uv pip install -r requirements.txt
#     # or, if using the bundled pyproject.toml:
#     uv sync
#
#   pipenv
#     pipenv install -r requirements.txt
#
#   mamba / conda
#     # mamba will fall back to PyPI for packages it cannot resolve on
#     # the configured channels. For a fully-native conda env see
#     # environment.yml alongside this file.
#     mamba create -n ultrack-viewer --file requirements.txt
#     mamba activate ultrack-viewer
#
# Tested against Python 3.12 / 3.13 on macOS (Apple Silicon) and Linux x86_64.
# =============================================================================

# ---- Core scientific stack ----------------------------------------------
numpy>=1.26
scipy>=1.12
pandas>=2.0

# ---- napari viewer + Qt backend -----------------------------------------
# [pyqt6] pulls in PyQt6 + QtPy, which the script imports directly
# (qtpy.QtWidgets, qtpy.QtCore).  Swap to [pyside2] if you prefer Qt6 LGPL.
napari[pyqt6]>=0.5
magicgui>=0.8
qtpy>=2.4

# ---- Plotting & visualisation -------------------------------------------
matplotlib>=3.8

# ---- Lazy / chunked image IO (image-stack support) ----------------------
dask>=2024.1
zarr>=2.15,<3           # ultrack segments.zarr is typically v2
tifffile>=2024.1

# ---- Video / frame export -----------------------------------------------
imageio>=2.34
imageio-ffmpeg>=0.6.0
